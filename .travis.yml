language: python
sudo: false
matrix:
  include:
    - python: "2.7"
    - python: "3.5"
    - python: "3.6"
    - python: "3.7"
    - python: "3.8"
      dist: xenial
      sudo: true
install:
    - pip install -U setuptools zc.buildout
    - buildout
script:
    - bin/test -v1
notifications:
    email: false
